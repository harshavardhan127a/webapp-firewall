# WAF Requirements
# Core
flask>=3.0.0
requests>=2.31.0
gunicorn>=21.0.0

# Storage backends
redis>=5.0.0  # Optional: for Redis storage backend

# Production
gevent>=23.0.0  # For async workers

# Anomaly Detection (optional — falls back to z-score if not installed)
scikit-learn>=1.3.0  # Optional: for IsolationForest anomaly detection

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
